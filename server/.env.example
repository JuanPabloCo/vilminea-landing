# Copy to .env and set your Mercado Pago access token to enable real preference creation
# Plain token (recommended for local dev):
# MP_ACCESS_TOKEN=YOUR_MERCADO_PAGO_ACCESS_TOKEN

# Optional encrypted token (production). If set and MP_ACCESS_TOKEN is empty, server will decrypt:
# MP_ACCESS_TOKEN_ENC=iv_base64:cipher_base64:tag_base64
# MP_DECRYPT_KEY=32_characters_key_for_aes_256_gcm

# Server port (default 3000):
# PORT=3000
